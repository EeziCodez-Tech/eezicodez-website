import { BackgroundPaths } from "@/components/ui/background-paths"

export default function DemoBackgroundPaths() {
    return (
        <main>
            <BackgroundPaths title="EeziCodez Background Paths" />
        </main>
    )
}
